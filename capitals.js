export const capitals = word => 
    // 1. "Атомный взрыв строки" (Спред-оператор)
    [...word] 
    /* 
      Представьте, что строка - это пиньята с буквами. 
      Спред-оператор ([...word]) - это детский праздник, 
      где все буквы вываливаются из строки как конфетти!
      Теперь каждая буква сидит в массиве отдельно, 
      как кот в коробке из "IKEA" 😼
    */
    .map((el, i) => 
      // 2. "Детектив РегистроВич расследует дело"
      (el === el.toUpperCase()) 
        ? i // "Ага, поймал заглавную букву с поличным! Индекс в клетку!"
        : undefined // "Прописная? Свободна, но под надзором!"
    )
    /*
      Тернарный оператор - это Шерлок Холмс в мире JS:
      "Элементарно, Ватсон! Если буква == uppercase, 
      возвращаем индекс, иначе - кот Шрёдингера (undefined)!"
    */
    .filter(el => el !== undefined);
    /*
      3. "Великая Чистка undefined"
      Фильтр здесь - как мать-героиня: 
      "Все undefined - марш из моего массива! 
      Индексы - остаётесь, вы герои дня!"
      В итоге получаем чистый список позиций заглавных букв, 
      как CVS-файл после "rm -rf node_modules" 💪
    */
  
  // Пример работы:
  // capitals("CodEx") → [0, 3]
  // Буква 'C' - как капитал корабля, 'E' - бунтарь в океане строчных!
